[package]
name = "actias-declarations"
version.workspace = true
edition = "2024"
license = "MIT"

[dependencies]
cron = "0.12"
mlua = { version = "0.11", features = ["luau"] }
serde = { version = "1.0.152", features = ["derive"] }
